<%= form_with(model: stash) do |form| %>
  <% if stash.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stash.errors.count, "error") %> prohibited this stash from being saved:</h2>

      <ul>
        <% stash.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
